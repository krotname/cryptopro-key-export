# Токены: применение, извлекаемость и покрытие тестами

Сводная таблица различает прямой опыт на живом устройстве и ожидания. Во всех
строках про экспорт проверяется целевой случай: контейнер создан на самом носителе,
а в `KP_PERMISSIONS` ещё нет `CRYPT_EXPORT`. Уже экспортируемый ключ КриптоПро
штатно копирует с любого подходящего носителя, и это не проверяет возможности утилиты.

Обозначения:

- 🔬 прямой физический опыт;
- 🧩 точная модель выведена из USB/прошивки/механизмов;
- 📋 только ожидание, живого устройства не было.

## Единая таблица

Доступность — фактический срез лаборатории на 27.08.2026. «Полностью (+)» означает
положительный E2E от неэкспортируемого контейнера на токене до PFX; «полностью (−)» —
исчерпывающе доказанную отрицательную границу; «частично» — живое устройство проверено,
но критический этап или production-путь ещё отсутствует.

| Модель | Доступность железа | Глубина проверки | Результат | Что требует внимания |
|---|---|---|---|---|
| **Рутокен S** | **подключён сейчас** (`VID_0A89/PID_0020`) | **полностью (+)** | Production APDU: 6 файлов, обе пары `…98 → …9C`, HDIMAGE и PFX; текущий носитель использован только как control | — |
| Рутокен ЭЦП 1.0 | живого экземпляра не было | **нет** | Ожидается аппаратная неизвлекаемость, но прямого опыта нет | Точная модель — низкий приоритет |
| **Рутокен Lite** | **подключён сейчас** (`VID_0A89/PID_0025`) | **полностью (+)** | fw 8.0/9.02: полный двухключевой E2E до двух PFX, `d·G==Q`; текущий носитель использован только как control | — |
| **Рутокен ЭЦП / профиль 2.x, fw 23.02** | **подключён сейчас** (`VID_0A89/PID_0030`) | **полностью (−)** | 46 механизмов; data/certificate CRUD, hardware RSA-2048 sign/verify; `CKA_NEVER_EXTRACTABLE=true`, `CKA_VALUE` unreadable, wrap → `CKR_KEY_NOT_WRAPPABLE`; cleanup → 0 объектов | Резервная копия аппаратного private key невозможна по конструкции |
| **Рутокен ЭЦП 3.0** | был доступен ранее | **полностью (−)** | CSP/PKCS#11/keycopy/wrap подтвердили: аппаратный ключ не покидает чип | —, резервная копия закрытого ключа невозможна по конструкции |
| **JaCarta PRO** | **подключена сейчас** (`VID_0529/PID_0620`) | **полностью (+)** | Exact selector ограничил protected read одним синтетическим контейнером; production APDU → 6 файлов → обе пары `…98 → …9C` → две HDIMAGE-копии → два PFX | — |
| **JaCarta LT** (`JaCarta DS`) | была доступна ранее | **полностью (+)** | Два независимых прогона: 6 файлов, обе пары `…98 → …9C`, HDIMAGE и PFX | — |
| **JaCarta IDProtect (Athena)** | была доступна ранее | **полностью (−)** | Ни контейнера КриптоПро, ни PKCS#11-токена; экспортировать нечего, диагностика fail-closed | — для этой платформы; не путать с PRO/LT |
| JaCarta-2 ГОСТ | живого экземпляра не было | **нет** | Общая PKCS#11-диагностика готова, модель не проверялась | Нужен точный живой носитель |
| **ESMART Token USB 64K** | был доступен ранее (`VID_072F/PID_90DE`) | **полностью (+)** | Файловый CSP-контейнер: APDU → обе пары → HDIMAGE → PFX; hardware PKCS#11 key остаётся неизвлекаемым | Другие ESMART-модели требуют отдельной физической валидации |
| **ESMART Token** | был доступен ранее (`VID_2CE4/PID_7479`) | **полностью (+)** | Code 10 закрыт; тот же полный E2E и аппаратная PKCS#11-граница | Другие ESMART-модели требуют отдельной физической валидации |
| eToken | живого точного экземпляра не было | **нет** | Production-путь не проверялся | Нужны точная модель, живой носитель и отдельный backend |

Итого: **12 модельных строк** — 6 полностью положительных, 3 полностью отрицательных,
0 частичных и 3 без физического теста; 4 точных носителя подключены сейчас.

## Что доказано физически

На Рутокен S/Lite, JaCarta LT/PRO и обоих ESMART использован одинаково строгий
положительный сценарий для пассивного файлового CSP-раздела:

1. точная идентификация USB и PC/SC reader;
2. создание отдельного синтетического двухключевого контейнера прямо на носителе;
3. baseline `0x00130898`/`0x00122898` без `CRYPT_EXPORT`;
4. чтение production APDU-бэкендом полного набора из шести файлов;
5. автоматическое извлечение обоих сертификатов и `p12utility --cprepair`;
6. точная HDIMAGE-установка и readback `0x0013089C`/`0x0012289C`;
7. целевой PFX через `certmgr`, принятый `certutil -dump` и OpenSSL;
8. удаление только синтетических контейнеров и тестовых артефактов.

Для JaCarta PRO дополнительно обязателен technical selector: при трёх публичных
контейнерах protected read получил только один разрешённый индекс. Два остальных
не открывались для protected read; их публичные имена после cleanup остались
неизменны.

Для активного Рутокен ЭЦП 2.x выполнен отдельный отрицательный сценарий на
исходно пустом расходном токене: object/certificate CRUD, hardware RSA-2048
generate/sign/verify, readback четырёх policy-атрибутов, отказ `CKA_VALUE` и
`C_WrapKey`, затем удаление всех пяти тестовых объектов и независимый нулевой
readback. Этот сценарий ничего не копирует с аппаратного private key.

Подробные протоколы:

- [Рутокен S](apdu/rutoken-s.md);
- [Рутокен Lite fw 9](apdu/rutoken-lite-fw9.md);
- [JaCarta LT](hardware/jacarta-lt.md);
- [JaCarta IDProtect](hardware/jacarta-idprotect.md);
- [ESMART Token USB 64K и ESMART Token](hardware/esmart-usb64k.md);
- [JaCarta PRO](apdu/jacarta-pro.md);
- [Рутокен ЭЦП 2.x fw 23.02](hardware/rutoken-ecp-fw23.md);
- Рутокен ЭЦП 3.0 — исторический физический опыт в [AGENTS.md](../AGENTS.md),
  пункты 27–31 и 36.

## Как читать тип носителя

Пассивный носитель хранит файловый контейнер, а криптооперации выполняет CSP на
компьютере. Флаг «неэкспортируемости» в таком случае является политикой CSP и может
быть обойдён чтением файловой памяти владельцем. Конкретные команды APDU различаются
у S, Lite, JaCarta LT, JaCarta PRO и ESMART, поэтому маршрутизация всегда fail-closed по типу.

Активный Рутокен ЭЦП вычисляет подпись внутри чипа; закрытый ключ не представлен
шестью файлами и аппаратно не покидает носитель. Для него утилита предоставляет
диагностику и извлечение публичного сертификата, но не обещает резервную копию ключа.

## Оговорка о рынке

Поштучные продажи по моделям и годам вендоры не раскрывают. Рыночные оценки могут
устаревать; технические выводы выше основаны на прямых физических проверках, а не
на популярности модели.

## Источники

- [Рутокен ЭЦП 3.0](https://www.rutoken.ru/products/all/rutoken-ecp-3/)
- [Драйверы Рутокен для Windows](https://www.rutoken.ru/support/download/windows/)
- [JaCarta LT](https://www.aladdin-rd.ru/catalog/jacarta/jacarta-lt/)
- [JaCarta Datastore в PKCS#11](https://developer.aladdin-rd.ru/pkcs11/2.4.1/guide/applets.html)
- [Поддержка ESMART Token](https://token.esmart.ru/support)
- [ESMART Token — PKCS#11](https://cdn.esmart.ru/token/docs/manuals/ESMART%20-%20PKCS11.pdf)
