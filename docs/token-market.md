# Модели Рутокена: рынок, применение и приоритет тестирования

Сводная таблица: когда появилась модель, какого она типа, где востребована сейчас, снимает ли
с неё утилита **закрытый** ключ и насколько её стоит брать для тестов. Объединяет три разбора
в один, чтобы держать перед глазами и дополнять.

## Единая таблица

Колонка **«Основание»** проставлена по каждой строке отдельно, чтобы не путать доказанное с ожидаемым:

- 🔬 **прямой опыт** — измерено на живом устройстве в этом проекте;
- 🧩 **модель определена по прошивке** — устройство тестировалось, но «это именно такая-то модель»
  выведено из прошивки и набора механизмов, а не отпечатано на корпусе;
- 📋 **только ожидание** — устройства на руках не было, вывод по аналогии, не проверен.

| Модель | ~Год | Тип носителя | Крипто на борту (подпись ГОСТ-2012) | Где востребована в 2026 | Утилита снимает **закрытый** ключ | Основание | Приоритет теста |
|--------|------|--------------|--------------------------------------|--------------------------|-----------------------------------|-----------|-----------------|
| **Рутокен S** | ~2004–2008 | активный (старый) | нет (только ГОСТ 28147 — шифр) | почти вытеснен, продаётся по инерции | **ожидается «да»** — файловый путь `rtCOMLite` (6 `*.key`) заложен, но на живом S **никогда не проверялся** | 📋 только ожидание | **1 — единственный носитель, где проверяется сама функция снятия** |
| Рутокен ЭЦП (1.0) | ~2011 | активный | ГОСТ Р 34.10-2001 | остаточно | ожидается «нет» (смарт-карта, как ЭЦП 2.0) — **на живом устройстве не проверялось** | 📋 только ожидание | низкий |
| **Рутокен Lite** | ~2013 | **пассивный** (хранилище) | нет | массовая: ФНС, госзакупки, маркировка — как хранилище ключа | **проверенными способами не снимается** — на живом 0 файлов через `rtCOMLite`; контейнеры CSP объектами PKCS#11 не становятся; APDU не пробовали | 🔬 прямой опыт (2 экз., fw 9.02 и 8.0) | покрыт |
| **Рутокен ЭЦП 2.0** | ~2015–2016 | активный | да, серт. ФСБ как СКЗИ | массовая: ЕГАИС; с 2026 — ФНС (новый ГОСТ) | **нет** — на живом токене оба закрытых ключа `CKA_EXTRACTABLE=false` (аппаратный флаг чипа), `csptest -keycopy` → `0x8009000b` | 🔬 прямой опыт · 🧩 «2.0» определена по fw 23.02 / 45 механизмов | покрыт |
| **Рутокен ЭЦП 3.0** | 2022 | активный | да + PKCS#11, 128 КБ | новейший стандарт, доля растёт | **нет — проверено прямым опытом**: на токене создан контейнер с двумя **неэкспортируемыми** ГОСТ-ключами, и ни один применимый к нему путь не сработал (`rtCOMLite`, `csptest -keycopy`, `certmgr -pfx`, команда `export`). PKCS#11 к этому контейнеру неприменим вовсе — созданный CSP контейнер объектом PKCS#11 не становится; неизвлекаемость чипа подтверждена **отдельным** опытом на своей PKCS#11-паре (`CKA_NEVER_EXTRACTABLE=true`, `C_WrapKey` → `CKR_KEY_NOT_WRAPPABLE`). Носитель — **ФКН**, ключ с диска на него ещё и не записать (`NTE_NOT_SUPPORTED`) | 🔬 прямой опыт · 🧩 «3.0» определена по fw 30.02 / 69 механизмов | **покрыт полностью** (fw 30.02): диагностика, CSP-free сертификат и опыт снятия ключа |
| **JaCarta PRO** (Аладдин Р.Д.) | ~2010 | активный | **нет** (только RSA/3DES/AES — 28 механизмов, ГОСТа на борту нет) | корпоративная аутентификация; для ЕГАИС/ФНС не годится | **проверенными способами не снимается** — опыт на своём контейнере с ключом, сгенерированным на носителе: `csptest -keycopy` и `certmgr -export -pfx` → `0x8009000b`, PKCS#11 объектов не показывает ни до `C_Login`, ни после, rtCOMLite считыватель не видит. APDU не пробовали | 🔬 прямой опыт (fw 1.01, PIN от владельца) | покрыт |
| JaCarta-2 ГОСТ (Аладдин Р.Д.) | — | активный | да | заметная доля: ЕГАИС/ФНС | не тестировалось (живого экземпляра не было; была JaCarta PRO — другая модель) | 📋 только ожидание (распознавание — общая `jcPKCS11-2.dll`) | по возможности |
| ESMART Token ГОСТ (ISBC) | — | активный | да | небольшая | не тестировалось; свой PKCS#11, в утилиту не заведён | 📋 только ожидание | по возможности |

**На каких экземплярах проверено.** Рутокен ЭЦП 2.0 — прошивка 23.02 (сеанс 12.08.2026);
Рутокен ЭЦП 3.0 — прошивка 30.02, аппаратная 60.01, 69 механизмов, включая RSA 512–4096 и ECDSA
(у 2.0 их нет — по ним модель и опознаётся, строка модели PKCS#11 у обеих одинаковая, `Rutoken ECP`);
Рутокен Lite — **два экземпляра, прошивки 9.02 и 8.0**; JaCarta PRO — прошивка 1.01, аппаратная 4.03
(всё — сеанс 13.08.2026). Оба Lite и ЭЦП 3.0 пришли с заводским PIN и без единого объекта PKCS#11;
у обоих Lite при этом лежат по три контейнера КриптоПро, видимых CSP, — лишнее подтверждение, что
контейнеры CSP объектами PKCS#11 не становятся. **JaCarta ведёт себя так же:** контейнеры КриптоПро
на ней CSP видит, а PKCS#11 не показывает ни одного объекта — ни до `C_Login`, ни после входа с
верным PIN, который сообщил владелец, и даже для контейнера, созданного тут же на месте.
Рутокен S не проверялся: устройства нет.

**На каком железе закрытый ключ извлекался успешно — уточнение: в целевом случае (с неснятым
запретом на экспорт) ни на каком.** Именно этот случай и есть смысл утилиты, и с физического токена
— Рутокен S/Lite/ЭЦП, JaCarta — такой ключ не снимался **ни разу за всю историю проекта**. Это видно
прямо по git: в первом же `ROADMAP.md` (коммит `c9cd82b`, 05.08.2026) строка снятия с токена помечена
«⚠️ **не проверено вживую**» (COM-цепочка доходит до `EnumReaders`, токен не был подключён), и статус
так и не стал «✅ на железе» — он прошёл путь «не проверено вживую» → «частично: на Lite файлов нет» →
«частично: на Lite/ЭЦП файлов нет». Поэтому в таблице нет и не должно быть строки «модель X, ключ с
неснятым запретом снят с токена успешно» — такой не существует.

**Оговорка, без которой ответ был бы неточным:** ключ, у которого запрет на экспорт **уже снят**
(`CRYPT_EXPORT` взведён), `csptest -keycopy` копирует с токена на диск штатно — 13.08.2026 это
прошло и на JaCarta, и на Рутокен Lite (см. абзац «Экспортируемый ключ — отдельная история» ниже).
Формально это закрытый ключ, физически покинувший токен, но это не обход защиты и не то, ради чего
существует утилита: экспортируемый ключ отдаёт любой носитель, вендор и модель тут ни при чём.

Что действительно работало на реальных данных с самых первых коммитов, но это **не** извлечение
ключа с токена:

- **извлечение `.cer`** из контейнера, уже установленного в CSP (первый коммит: «извлёк живой
  сертификат из контейнера в CSP») — это сертификат, а не закрытый ключ, и через CSP, а не с токена;
- цикл **снятие запрета → установка в CSP → PFX** — проверен end-to-end на **синтетическом файловом
  контейнере** (`csptest -newkeyset`), без токена и без боевого ключа.

Единственный случай, где **закрытый ключ с неснятым запретом действительно извлечён**, — CSP-free
разбор файлов контейнера (06.08.2026, три реальных контейнера, сверка с сертификатом по `X`, `Y` и
отпечатку `header.key`, раздел P1 и `csp-free-core`). Но это разбор **файлового контейнера на диске**,
а не снятие с физического токена — к строкам этой таблицы (модели токенов) он не относится и потому
здесь не отмечен. (Экспортируемый ключ через `csptest -keycopy` — не в счёт: см. оговорку выше.) Для токенов же вывод по всему парку: ключ либо аппаратно неизвлекаем (ЭЦП 2.0/3.0), либо не
отдаётся проверенными способами (Lite, JaCarta), либо путь заложен, но не проверен (Рутокен S).

**Экспортируемый ключ — отдельная история, и её легко спутать с главной.** Если запрет на экспорт
уже снят, КриптоПро копирует ключ с носителя на диск штатным `csptest -keycopy` — 13.08.2026 это
прошло **и на JaCarta, и на Рутокен Lite** (обе операции код 0, у копий `KP_PERMISSIONS =
0x0013089C`). Разницы между вендорами тут нет, и никакого обхода защиты в этом нет тоже. Все
формулировки в таблице относятся к ключу с **неснятым** запретом — именно на нём CSP отвечает
`0x8009000b`, и именно этот случай интересен проекту. Поэтому годный образец получают так:
контейнер создаётся **прямо на носителе** (`token-session\make-token-container.ps1`, ключ
генерируется на месте) — тогда запрет заведомо на месте, и результат что-то значит.

**Опознаётся ли носитель утилитой.** Одна библиотека PKCS#11 показывает только своего вендора:
на машине с четырьмя считывателями `rtPKCS11ECP.dll` отдала три слота (все Рутокены) и не увидела
JaCarta, а `jcPKCS11-2.dll` — ровно один слот (только JaCarta). Поэтому утилита с версии 1.4.3 ищет
и загружает обе библиотеки и объединяет слоты; до этого JaCarta не попадала в `deps`/`list`/`token`
вообще. Модель у JaCarta в PKCS#11 — просто `PRO`, поэтому семейство определяется по производителю
(`Aladdin R.D.`), а не только по строке модели.

## Как читать

- **Тип носителя.** «Пассивный» (Lite) — это просто защищённая память: ключ хранится, но криптооперации
  делает CSP на компьютере. «Активный» — чип сам считает подпись, ключ его не покидает.
- **Снятие ключа.** Работает только там, где ключ лежит **файлами** в памяти носителя (Рутокен S):
  `rtCOMLite` читает шесть `*.key` мимо CSP, флаг «неэкспортируемости» игнорируется. На смарт-карточных
  моделях (Lite, ЭЦП 2.0/3.0, JaCarta PRO) файловой раскладки нет, читать нечего. Подробности и
  доказательства — `AGENTS.md`, пп. 20–22, 25–26, 32–35; матрица с формулировками — `README.md`,
  раздел «На каких носителях снимается закрытый ключ».
- **Приоритет теста.** Отражает не популярность модели, а ценность для проверки кода. Массовые токены
  (Lite, ЭЦП 2.0 и 3.0) уже покрыты. На всех проверяется **диагностика** (тип, серийник, прошивка, PIN,
  контейнеры). **Извлечение сертификата без CSP** опирается на то, что публичные объекты PKCS#11
  (`CKA_PRIVATE=false`) читаются без `C_Login`; на живом железе это подтверждено **и на ЭЦП 3.0
  (fw 30.02), и на Lite (fw 9.02)** — объекты с `CKA_PRIVATE=true` в той же сессии не видны.
  Практически сертификат оказывается на ЭЦП чаще: у виденных Lite объектов PKCS#11 не было вовсе, но
  **хранилище объектов у Lite рабочее** — оно принимает и `CKO_DATA`, и `CKO_CERTIFICATE`, так что
  выданный УЦ через PKCS#11 сертификат утилита сняла бы и с него. Снятие **закрытого** ключа не
  проверяется ни на одном из них: на Lite проверенные способы его не дают, на ЭЦП 2.0 он неизвлекаем
  аппаратно. **На ЭЦП 3.0 и JaCarta PRO снятие проверено прямым опытом** — на контейнерах, ключи
  которых сгенерированы на самом носителе, — и не удалось ни одним из проверенных путей.
  Рутокен S нужен только чтобы закрыть e2e самой функции снятия.
- **ФКН.** Рутокен ЭЦП 3.0 КриптоПро CSP 5.0 R2 обслуживает как функциональный ключевой носитель
  (`rutoken_fkc_<серийник>`): ключ полагается **генерировать на носителе**, а импорт готового ключа
  с диска CSP отклоняет (`CopyPrivateKey → 0x80090029`). На Lite и в HDIMAGE то же копирование
  проходит. Для проекта это ещё один довод, почему направление «снять ключ с ЭЦП» закрыто: на такой
  носитель ключ даже не положить извне.

## Вывод, который стоит держать в голове

Функция «снять закрытый ключ» имеет смысл почти исключительно для **унаследованных Рутокен S**, а рынок
давно ушёл на смарт-карточные ЭЦП 2.0/3.0 и Lite. С **ЭЦП 2.0** ключ неизвлекаем аппаратно
(`CKA_EXTRACTABLE=false` в чипе), а **ЭЦП 3.0** CSP вообще ведёт как ФКН — на него ключ с диска не
записать. С **Lite** его не дают проверенные способы (`rtCOMLite`, PKCS#11, `csptest`), но APDU-путь
через собственный PC/SC-стек CSP не исследован, поэтому «в принципе невозможно» про Lite говорить рано
— точная формулировка «проверенными способами не снимается». **JaCarta PRO** проверена так же и с
тем же исходом: на контейнере, ключ которого сгенерирован на самом носителе, и `csptest -keycopy`,
и `certmgr -export -pfx` отвечают `0x8009000b`, а PKCS#11 не показывает объектов даже после входа
по PIN. APDU-путь на ней тоже не исследован.
В любом случае основная ценность утилиты на сегодняшнем парке устройств — **CSP-free работа с
сертификатом и диагностика**. Диагностика подтверждена на всех четырёх проверенных моделях
(включая JaCarta), CSP-free сертификат — на Рутокен ЭЦП 2.0/3.0 и Lite; на JaCarta он недоступен,
потому что объектов PKCS#11 там нет вовсе.

## Оговорка о данных

Поштучные продажи по моделям и годам вендоры не раскрывают — это коммерческая тайна; годы появления
моделей здесь **ориентировочны**. Твёрдо подтверждены только: «Актив» (Рутокен) — лидер рынка РФ,
выпускает **>1 млн устройств в год**; Рутокен ЭЦП 3.0 — с 2022 (серт. ФСТЭК 09.2022); требования
гос-систем к типу токена (ЕГАИС и, с 2026, ФНС — активный со встроенным СКЗИ). Факты про снятие ключа
— не с рынка, а из прямых проверок на живых устройствах (см. ссылки выше).

## Источники

- [Компания «Актив» — TAdviser](https://www.tadviser.ru/index.php/Компания:Актив_(Актив-софт))
- [Рутокен ЭЦП 3.0 — rutoken.ru](https://www.rutoken.ru/products/all/rutoken-ecp-3/)
- [Обзор линейки Рутокен ЭЦП 3.0 — cryptostore](https://cryptostore.ru/article/novosti/obzor_lineyki_rutoken_etsp_3_0/)
- [Отличия Рутокен Lite/S/ЭЦП 2.0 — ITCOM](https://uc-itcom.ru/help/docs/otlichiya-rutoken-lite-rutoken-s-rutoken-eczp-2-0)
- [Токены для ФНС — Роселторг](https://www.roseltorg.ru/knowledge_db/ecp-info/article/tokeny-dlya-fns-kakie-byvayut-i-kak-vybrat)
- [Токены с ЭЦП для налоговой — Астрал](https://astral.ru/info/nositeli-etsp/tokeny-dlya-fns-kakie-byvayut-i-kak-vybrat/)
- [Новый ГОСТ шифрования для ФНС — Контур](https://www.kontur-extern.ru/info/84017-novyy_gost_shifrovaniya)
- [Замена Рутокен ЭЦП 2.0 для ФНС — ecp-shop](https://ecp-shop.ru/services/zamena-rutoken-ecp-2-0-s-1-aprelya-2026-goda-dlya-raboty-s-fns/)
- [Рынок средств аутентификации РФ — Anti-Malware](https://www.anti-malware.ru/node/11550)
